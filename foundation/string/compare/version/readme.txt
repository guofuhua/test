比较版本号字符串"2.3.05.0811"与"2.3.05.0722"的版本大小，获取较大的版本号
1,int compare(QString strVersion1, QString strVersion2);
比较两个纯版本号字符串的大小
2,bool isVersionStr(QString strVersion);
判断字符串是不是纯版本号字符串
3,QString getLargeVersion(QString V1, QString V2);
获取较大的版本号
